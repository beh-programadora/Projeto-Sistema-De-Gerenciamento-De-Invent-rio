# Arquivos e diretórios a serem ignorados pelo Git

# Arquivos de banco de dados
*.sql

# Arquivos de configuração
*.conf

# Arquivos temporários
*.tmp
*.log

# Diretórios temporários
tmp/
log/
Enunciado do Projeto
Título: Sistema de Gerenciamento de Inventário para Armazém

Objetivo: Desenvolver um sistema de gerenciamento de inventário para um armazém. O sistema deve permitir o controle e monitoramento de produtos, categorias e fornecedores. O banco de dados deve suportar a inserção, consulta, atualização e exclusão de dados.

Requisitos:

Banco de Dados:

Criar um banco de dados chamado InventarioArmazem.
Implementar tabelas para Categorias, Fornecedores e Produtos.
Relacionamentos:

As tabelas Produtos e Categorias devem estar relacionadas através de uma chave estrangeira.
As tabelas Produtos e Fornecedores devem estar relacionadas através de uma chave estrangeira.
Dados de Exemplo:

Inserir dados de exemplo nas tabelas Categorias, Fornecedores e Produtos.
Consultas SQL:

Consultar todos os dados disponíveis no estoque.
Mostrar todos os produtos que pertencem a uma categoria específica.
Exibir informações sobre os fornecedores que fornecem produtos acima de um determinado preço.
Atualizações e Exclusões:

Atualizar o preço de um produto específico.
Modificar a descrição de uma categoria existente.
Alterar o endereço de um fornecedor específico.
Remover um produto do inventário.
